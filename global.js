/*
// Global object
// console.log(global);

// Runs once after the specified time
global.setTimeout(() => {
    console.log("in the timeout");
    clearInterval(int);
}, 3000);

// Runs after a certain interval continuously
const int = setInterval(() => {
    console.log("in the interval");
}, 1000);
*/

console.log(__dirname);
console.log(__filename);